server:
  port: 9003
spring:
  application:
    name: potato-server
  cloud:
    consul:
      host: ${CONSUL_HOST:10.224.112.67}
      port: ${CONSUL_PORT:8500}
      discovery:
        preferIpAddress: true