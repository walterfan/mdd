server:
  port: 9003
spring:
  application:
    name: potato-web
  cloud:
    consul:
      host: ${CONSUL_HOST:127.0.0.1}
      port: ${CONSUL_PORT:8500}
      config:
        enabled: true
      discovery:
        preferIpAddress: true